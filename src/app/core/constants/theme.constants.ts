import { ITheme } from '../models/theme.interface';

const THEME_PATH = '/assets/theme';

export const THEME: ITheme = {
  dark: `${THEME_PATH}/dark.css`,
  light: `${THEME_PATH}/light-material.css`
};
